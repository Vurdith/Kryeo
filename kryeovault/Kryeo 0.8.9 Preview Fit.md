# Kryeo 0.8.9 Preview Fit

- Locked asset-preview image sizing to `contain` with centered positioning at the final stylesheet layer.
- Prevents later CSS rules from stretching or cropping preview images.
