# Kryeo 0.8.10 Preview Correction

- Moved the preview fit rule to the final stylesheet layer.
- Added inline `contain` and centered positioning to every rendered asset preview.
- Prevents legacy CSS from stretching square assets to the preview frame.
