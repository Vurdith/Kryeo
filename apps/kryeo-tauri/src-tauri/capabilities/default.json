{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimum capability set for Kryeo's desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
